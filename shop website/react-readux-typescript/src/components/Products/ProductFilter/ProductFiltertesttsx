import React from 'react'
import { renderWithStore, screen } from '../../test-utils/testUtils'

describe('ProductFilter',()=>{
    it('should render form with all filter options', () => {
        renderWithStore()
        screen.getBy
    })
    
})